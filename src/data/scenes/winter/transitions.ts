
import { Scene } from '../../../types/game';

// This file is for symmetry with other seasons, but winter doesn't transition to another season
// Instead, it transitions to epilogue which is handled by the winter conclusion scenes
const winterTransitions: Record<string, Scene> = {};

export default winterTransitions;
